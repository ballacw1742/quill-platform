fastapi>=0.110
uvicorn[standard]>=0.27
anthropic[vertex]>=0.40
# [socks] pulls socksio for the LOCAL_INFERENCE_PROXY path (Tailscale
# userspace SOCKS5 sidecar reaching on-prem ollama, §8 model-lane router).
httpx[socks]>=0.27
sqlalchemy[asyncio]>=2.0
asyncpg>=0.29
greenlet>=3.0
pydantic>=2.6
pydantic-settings>=2.2
# A4: cron next-run computation for agentcloud_schedules
croniter>=2.0
# A3: only imported when EVENT_BUS=pubsub / JOBS_BACKEND=cloudrun (lazy imports)
google-cloud-pubsub>=2.21
google-cloud-run>=0.10
# Phase 4 §9.5: age on-prem KEK (SECRETS_BACKEND=age, ONPREM.md).
# Optional — only needed for the age backend; lazy-imported inside app/secrets.py
# via _load_pyrage(). All other backends work without it.
pyrage>=0.2
# Phase F: Google Drive/Docs/Sheets deliverable authoring (DRIVE_ENABLED).
# Optional — lazy-imported inside app/drive_author.py; all other backends work
# without them. Install these alongside requirements.txt to enable live Drive authoring.
google-api-python-client>=2.100
google-auth>=2.23
